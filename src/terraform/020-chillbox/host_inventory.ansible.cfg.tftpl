# Generated from ${template_source_file} in the local terraform 020 chillbox container.

%{ for hostname, ipv4_address in chillbox_name_ipv4address_map ~}
${hostname} ansible_host=${ipv4_address}
%{ endfor ~}

[all:vars]
tech_email=${tech_email}

[chillbox]
%{ for hostname, _ in chillbox_name_ipv4address_map ~}
${hostname}
%{ endfor ~}

[chillbox:vars]
domain_name=${sub_domain}${domain}
